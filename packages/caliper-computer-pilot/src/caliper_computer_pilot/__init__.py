"""caliper-computer-pilot — caliper adapter for the cu CLI.

**Status: Phase 3a skeleton.** Module placeholders only. Implementation
plan mirrors caliper-browser-pilot:

    cu_agent              — solver factory with cu defaults baked in
    CU_OBSERVATION_COMMANDS — set of cu sub-commands that count as observation
    cu_truncate_snapshot   — cu-specific snapshot → compact text
    cu_skill_path          — locate cu's SKILL.md

See roadmap milestone M3a in ``docs/roadmap.md``.
"""
